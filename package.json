{
  "name": "axis-client",
  "version": "1.0.0",
  "description": "fire up the ovens",
  "main": "./web/index.html",
  "dependencies": {
    "jquery": "^3.5.1",
    "js-cookie": "^2.2.1",
    "sockette": "^2.0.6",
    "websocket-as-promised": "^1.1.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --public-url '.' src/websrc/index.html",
    "clean": "rm web/* 2>/dev/null || exit 0",
    "rebuild": "npm run clean && npm run build",
    "rebuild-full": "rm -rf .parcel-cache/ && npm run rebuild"
  },
  "browserslist": [
    "last 1 Chrome version"
  ],
  "author": "",
  "license": "MIT"
}
